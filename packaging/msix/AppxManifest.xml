<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX manifest for the Microsoft Store build.

  Identity matches the Partner Center reservation (Store ID 9NS238QVJ9CS).
  Version's fourth field must stay 0 for Store submissions.

  Full-trust packaged desktop app (rescap:runFullTrust). Unlike the sibling IME
  tool, this one needs no elevation, so the Store build is fully functional. The
  one thing to confirm is that RegLoadAppKey on Snipping Tool's settings.dat
  works from within the package (see packaging/msix/README.md).
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:uap5="http://schemas.microsoft.com/appx/manifest/uap/windows10/5"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap uap5 rescap">

  <Identity
    Name="MangoYen.HDRScreenshotSyncer"
    Publisher="CN=B64E145E-DB3F-473D-9BA6-BDF6CF2E8081"
    Version="0.2.2.0" />

  <Properties>
    <DisplayName>HdrScreenshotSyncer</DisplayName>
    <PublisherDisplayName>Mango Yen</PublisherDisplayName>
    <Logo>assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.22621.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="HdrScreenshotSyncer" Executable="HdrScreenshotSyncer.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="HdrScreenshotSyncer"
        Description="Keeps Snipping Tool's HDR screenshot color corrector in sync with the display's HDR state."
        BackgroundColor="transparent"
        Square150x150Logo="assets\Square150x150Logo.png"
        Square44x44Logo="assets\Square44x44Logo.png">
        <uap:DefaultTile
          Wide310x150Logo="assets\Wide310x150Logo.png"
          Square71x71Logo="assets\Square71x71Logo.png"
          Square310x310Logo="assets\Square310x310Logo.png" />
      </uap:VisualElements>
      <Extensions>
        <!-- Autostart the packaged way; the user enables it in Settings > Startup.
             An HKCU Run key would be virtualized to no effect under MSIX. -->
        <uap5:Extension Category="windows.startupTask" Executable="HdrScreenshotSyncer.exe" EntryPoint="Windows.FullTrustApplication">
          <uap5:StartupTask TaskId="HdrScreenshotSyncerStartup" Enabled="false" DisplayName="HdrScreenshotSyncer" />
        </uap5:Extension>
      </Extensions>
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
